#include <bits/stdc++.h>
using namespace std;

#define endl "\n"

bool ispowOf2(int x)
{
    if (x & (x - 1) == 0)
    {
        return true;
    }
    else
    {
        return false;
    }
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    return 0;
}